apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
spec:
  type: LoadBalancer             # GKE provisions an external IP
  selector:
    app: fastapi                 # Matches pods with this label
  ports:
    - protocol: TCP
      port: 80                   # Port exposed outside the cluster
      targetPort: 80             # Port pods are listening on
